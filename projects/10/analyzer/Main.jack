class SquareGame {

   /** Runs the game: handles the user's inputs and moves the square accordingly */
   method void run() {
         while (~(key = 0)) {
            let key = Keyboard.keyPressed();
            do moveSquare();
         }
		}
}



